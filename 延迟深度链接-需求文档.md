# 延迟深度链接 — 需求文档
## 1. 概述
Meta 自动化任务账户包「应用推广」模式（投放策略=直投）下，选择应用后新增「延迟深度链接」开关，radio 形态（关闭/开启），默认关闭。仅布尔开关，不新增输入框，链接值取应用自带配置。
## 2. 背景
应用推广投放需支持延迟深度链接跳转场景，复用应用已配置的深度链接能力。当前账户包直投策略无该开关，需补充。
## 3. 变更内容
### 3.1 新增账户包抽屉
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 新增账户包抽屉（`#apStrategy` 投放策略行下方） | 不变 |
| 字段 | 投放策略为「直投」时无附加字段 | 新增「选择应用」下拉（必填）+「延迟深度链接」开关（默认关闭） |
- 投放策略选「直投」时显示选择应用与延迟深度链接；选 W2A / H5 时不显示，维持现状（行动号召、像素ID）
- 切换投放策略实时联动：直投 ↔ W2A/H5 显示不同字段集
### 3.2 选择应用
- 下拉单选，mock 仅一个应用（Netshort），必填
- 选择结果决定延迟深度链接的生效范围，切换不触发其余字段变化
### 3.3 延迟深度链接开关
- radio 形态，关闭 / 开启二选一，默认关闭
- 开启后不展示额外输入项，延迟深度链接取应用自带配置
### 3.4 提交
账户包创建/提交携带 `selectApp` 与 `delayedDeepLink`（关闭 0 / 开启 1）。
## 4. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 账户包抽屉新增选择应用与延迟深度链接开关、直投联动 |
