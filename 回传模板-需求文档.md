# 回传模板 — 需求文档
## 1. 概述
回传模板决定 Meta / TikTok 流量回传策略（全回传 / 扣量 / IAA 等），待选名单由管理后台接口按 媒体 × 投放策略 × 业务类型 维度返回。Meta 自动化任务（auto_put）与 TikTok 自动化任务（auto_put_tiktok）均需在表单补充回传模板录入，两项目分别调整：
| 项目 | 表单位置 | 字段位置 | 提交字段 |
|------|---------|---------|---------|
| Meta | 「新增账户包」侧边栏 | 「投放策略」之后 | `template`（回传模板名称） |
| TikTok | 投放配置区 | 「广告系列名称后缀」之后 | account 级 `callbackTemplate`（回传模板 id） |
## 2. 背景
回传模板决定流量回传策略（全回传 / 扣量 / IAA 等），待选名单由监控接口 `POST /video/callback_plan/monitorPage` 按 媒体(mediaSource) × 投放策略(strategy) × 业务类型(businessType) 维度返回，仅返回启用模板。当前 Meta「新增账户包」表单与 TikTok 任务表单均未提供回传模板选择，需补充。
## 3. Meta 项目（auto_put）
### 3.1 变更内容 — 新增账户包侧边栏
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 新增账户包侧边栏（`#accountPackageDrawer`） | 不变 |
| 字段 | 无回传模板相关字段 | 「投放策略」之后新增「回传模板」下拉（必填，无默认） |
- 账户包表已含「回传模板」列，补上表单录入项，使新增账户包可携带回传模板
### 3.2 下拉数据来源
待选名单由监控接口 `POST /video/callback_plan/monitorPage` 按以下参数筛选：
| 参数 | 取值 | 说明 |
|------|------|------|
| `mediaSource` | `1` | Meta |
| `strategy` | `1`=W2A / `2`=直投 / `4`=H5 | 与「投放策略」按钮组联动 |
| `businessType` | `1`=免费 / `2`=付费 | 与账户包「业务类型」一致 |
| `enableStatus` | `1` | 仅启用状态 |
- 投放策略=直投 → `strategy=2`；W2A → `strategy=1`；H5 → `strategy=4`
- 业务类型=免费 → `businessType=1`；付费 → `businessType=2`
- 投放策略=H5 且 业务类型=付费 时无待选名单，下拉仅剩占位项
### 3.3 表单联动
- 切换投放策略 / 业务类型时，重新请求待选名单并清空已选项
- 打开「新增账户包」抽屉时按当前策略与业务类型初始化待选名单
### 3.4 提交
账户包创建时新增字段 `template`（回传模板名称），回传模板下拉选中值落库到账户包回传模板列。
### 3.5 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 新增账户包侧边栏新增回传模板下拉、联动逻辑、提交字段 |
## 4. TikTok 项目（auto_put_tiktok）
### 4.1 变更内容 — 投放配置区
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 投放配置区（`#appPromotionConfig` 之外） | 不变 |
| 字段 | 无回传模板相关字段 | 「广告系列名称后缀」之后新增「回传模板」下拉（必填，无默认） |
- 可见条件：应用推广与销量均显示；推广系列类型为 TikTok Minis 时隐藏
### 4.2 下拉数据来源
待选名单由监控接口 `POST /video/callback_plan/monitorPage` 按以下参数筛选：
| 参数 | 取值 | 说明 |
|------|------|------|
| `mediaSource` | `2` | TikTok |
| `strategy` | `1`=W2A / `2`=直投 / `4`=H5 | 由推广目标×投放策略决定 |
| `businessType` | `1`=免费 / `2`=付费 | 与任务业务类型一致 |
| `enableStatus` | `1` | 仅启用状态 |
- 推广目标=应用推广 → 投放策略固定直投，`strategy=2`
- 推广目标=销量 → 投放策略 W2A/H5，`strategy` 随投放策略联动
### 4.3 表单联动
- 切换推广目标 / 投放策略 / 业务类型时，重新请求待选名单并清空已选项
- 推广系列类型切换为 TikTok Minis 时隐藏该字段并清空已选项
### 4.4 提交
任务提交时在 `account` 级新增字段 `callbackTemplate`（回传模板 id）。
### 4.5 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 投放配置区新增回传模板下拉、联动逻辑、提交字段 |