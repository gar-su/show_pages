# 投放账户模块

## 概述

新建 Meta 自动化任务页面新增投放账户模块，用于选择投放账户及定向包。模块位于素材配置模块下方。

## 位置

素材配置模块与素材预览区域之间。

## 字段

### 投放账户（必填）

| 属性 | 值 |
|------|-----|
| 字段名 | `account.package` |
| 类型 | string |
| 组件 | 下拉选择框 |
| 选项 | 账户包列表（由后端提供） |
| 默认提示 | 请选择账户包 |

### 选择定向包（可选）

| 属性 | 值 |
|------|-----|
| 字段名 | `account.targetingPackage` |
| 类型 | string / null |
| 组件 | 下拉选择框 |
| 选项 | 定向包列表（由后端提供） |
| 默认提示 | 在此处选择定向包将会覆盖账户内定向包 |
| 说明 | 不选时提交 `null`；选择后将覆盖账户包内原有定向包 |

## 提交数据格式

```json
{
  "account": {
    "package": "账户包A-北美投放",
    "targetingPackage": "定向包1-兴趣标签"
  }
}
```

未选择定向包时：

```json
{
  "account": {
    "package": "账户包A-北美投放",
    "targetingPackage": null
  }
}
```

## 校验规则

- 投放账户为必填项，未选择时提示"请选择投放账户"

## 验收标准

1. 投放账户模块位于素材配置下方、素材预览上方
2. 投放账户为必填下拉框，默认显示"请选择账户包"
3. 选择定向包为可选下拉框，默认显示"在此处选择定向包将会覆盖账户内定向包"
4. 提交时投放账户未选择则阻止提交并提示
5. 选择定向包未选择时提交 `null`
