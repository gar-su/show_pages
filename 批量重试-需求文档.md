# 批量重试 — 需求文档
## 1. 概述
广告系列、广告组、广告三个列表支持批量选中重试。列表支持逐行勾选与表头全选，选中上限为当页列表数量；「上传成功」条目不支持重试；批量重试需二次确认后执行。
## 2. 背景
批量补偿上传失败的广告实体是高频操作，当前仅支持逐条重试，需补充批量能力，降低运营成本。
## 3. 变更内容
### 3.1 批量操作栏
- 位置：三个列表表格上方（`#campaignTableBody` / `#adgroupTableBody` / `#adTableBody` 对应列表）
- 组成：「已选择 N 条」计数 + 「批量重试」按钮
- 联动：勾选实时更新计数；表头全选勾选/取消当页全部可重试条目
### 3.2 选择范围
- 仅限当前页：翻页后选择清空、计数归零
- 表头全选仅作用于当页可重试条目
### 3.3 重试规则
- 「上传成功」条目不支持重试：行 checkbox 禁用、行内「重试」按钮置灰
- 点击「批量重试」：先校验已选数量，未选择时提示；二次确认后执行，完成后清空选择
### 3.4 上传状态
- 广告组列表新增「上传状态」列（上传成功/上传失败），与广告系列、广告一致
- 广告系列、广告原有上传状态列不变
## 4. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 批量操作栏、批量选择逻辑、上传成功禁选禁点、广告组上传状态列 |
