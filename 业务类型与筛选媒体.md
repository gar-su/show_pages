# 素材数据筛选 — 新增「业务类型」与「筛选媒体」
## 概述
素材数据筛选模块新增两个筛选维度：业务类型（不限/免费/付费）与投放媒体（不限/Meta/TikTok）。两个维度仅在素材数据筛选选择「自定义」时显示，选择「不限」时隐藏。
## 页面改动
### 位置
素材数据筛选区域，「不限/自定义」按钮组下方、「素材数量」上方。
### 字段
#### 业务类型
| 属性 | 值 |
|------|-----|
| 字段名 | `material.businessType` |
| 类型 | string |
| 组件 | 按钮组 |
| 选项 | 不限、免费、付费 |
| 默认 | 不限 |
| 可见条件 | 素材数据筛选 = 自定义 |
#### 筛选媒体
| 属性 | 值 |
|------|-----|
| 字段名 | `material.media` |
| 类型 | string |
| 组件 | 按钮组 |
| 选项 | 不限、Meta、TikTok |
| 默认 | 不限 |
| 可见条件 | 素材数据筛选 = 自定义 |
## 3. 数据需求
> 本章节用于与大数据开发对齐，评审时逐条确认。
### 3.1 字段说明
| 项 | 业务类型 | 筛选媒体 |
|----|---------|---------|
| 字段名 | `business_type`（待大数据确认最终命名） | `media`（待大数据确认最终命名） |
| 字段含义 | 区分素材归属的业务线 | 区分素材投放的媒体渠道 |
| 数据类型 | `VARCHAR(20)` | `VARCHAR(20)` |
| 枚举值 | `free`（免费）、`paid`（付费）、`unlimit`（不限） | `meta`（Meta）、`tiktok`（TikTok）、`unlimit`（不限） |
| 数据来源 | 上游投放系统回流标签 | 上游投放系统回流标签 |
| 关联键 | `material_id`（素材 ID） | `material_id`（素材 ID） |
| 默认值 | 历史数据默认为 `unlimit` | 历史数据默认为 `unlimit` |
### 3.2 数据清洗规则
| 规则 | 判定条件 | 处理方式 |
|------|---------|---------|
| 非法枚举值 | 字段值不在上述枚举范围内 | 置为 `unlimit`，写入清洗日志 |
| NULL 值 | 字段值为 NULL | 视为 `unlimit`，参与筛选 |
### 3.3 待与大数据确认项
| # | 确认项 | 待定 |
|---|--------|------|
| 1 | 素材数据源表是否有现成的业务类型/媒体字段？若有，字段名和枚举值是什么？ | |
| 2 | 若无现成字段，从哪个上游系统回流？数据链路是否已有规划？ | |
| 3 | 历史数据回填策略：全量默认 `unlimit`，还是按投放记录回溯打标？ | |
| 4 | 同一个素材是否可能同时属于多个业务类型或媒体？若可能，如何存储？ | |
| 5 | 字段数据产出延迟（T+1 / 实时）？ | |
### 3.4 接口约定
素材列表查询接口（及导出接口）需在每条素材记录中增加以下字段：
```json
{
  "material_id": "M20260527001",
  "business_type": "free",
  "media": "meta"
}
```
筛选协议：前端传参 `businessType` / `media` 时，后端映射到 `business_type` / `media` 字段执行筛选。
## 提交数据格式
```json
{
  "material": {
    "businessType": "free",
    "media": "meta"
  }
}
```
不限时提交 `"unlimit"`。
## 验收标准
1. 素材数据筛选选择「不限」时，业务类型和筛选媒体不显示
2. 素材数据筛选选择「自定义」时，业务类型和筛选媒体显示
3. 「业务类型」按钮组默认选中「不限」，三个选项互斥，点击切换
4. 「筛选媒体」按钮组默认选中「不限」，三个选项互斥，点击切换
5. 切换选项时触发素材预览重置
6. 提交时 `businessType` 和 `media` 字段正确包含在 `material` 对象中
