# 素材数据筛选 — 新增「业务类型」与「筛选媒体」
## 概述
素材数据筛选模块新增两个筛选维度：业务类型（不限/免费/付费）与投放媒体（不限/Meta/TikTok）。两个维度仅在素材数据筛选选择「自定义」时显示，选择「不限」时隐藏。
## 页面改动
### 位置
素材数据筛选区域，「不限/自定义」按钮组下方、「素材数量」上方。
### 字段
#### 业务类型
| 属性 | 值 |
|------|-----|
| 字段名 | `material.businessType` |
| 类型 | string |
| 组件 | 按钮组 |
| 选项 | 不限、免费、付费 |
| 默认 | 不限 |
| 可见条件 | 素材数据筛选 = 自定义 |
#### 筛选媒体
| 属性 | 值 |
|------|-----|
| 字段名 | `material.media` |
| 类型 | string |
| 组件 | 按钮组 |
| 选项 | 不限、Meta、TikTok |
| 默认 | 不限 |
| 可见条件 | 素材数据筛选 = 自定义 |
## 提交数据格式
```json
{
  "material": {
    "businessType": "free",
    "media": "meta"
  }
}
```
不限时提交 `"unlimit"`。
## 验收标准
1. 素材数据筛选选择「不限」时，业务类型和筛选媒体不显示
2. 素材数据筛选选择「自定义」时，业务类型和筛选媒体显示
3. 「业务类型」按钮组默认选中「不限」，三个选项互斥，点击切换
4. 「筛选媒体」按钮组默认选中「不限」，三个选项互斥，点击切换
5. 切换选项时触发素材预览重置
6. 提交时 `businessType` 和 `media` 字段正确包含在 `material` 对象中
