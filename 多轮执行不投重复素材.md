# 多轮执行不投重复素材

## 概述

定时任务多轮执行时，已投过的素材不再重复投放。前端增加开关控制，后端负责存储已投素材并去重。

## 前端

### 位置

素材配置模块，素材重复次数与广告数之间。

### 开关

| 状态 | 提交值 |
|------|--------|
| 关闭（默认） | `false` |
| 开启 | `true` |

提交字段：`material.dedupAcrossRounds`，布尔类型。

## 后端处理

### 数据存储

任务表新增字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `dedup_across_rounds` | boolean | 是否开启多轮去重 |
| `delivered_materials` | json/text | 已投放素材 ID 列表 |

### 执行流程

```
每轮执行：
  1. 查询素材池（按筛选条件）
  2. 如果 dedup_across_rounds = true：
     a. 读取 delivered_materials
     b. 从素材池剔除已投放的素材
  3. 从去重后的素材池中选取素材投放
  4. 将本次投放的素材 ID 追加到 delivered_materials
  5. 继续下一轮
```

### 边界情况

- `delivered_materials` 为空或不存在时，视为空列表，正常投放
- 素材池全部重复时，按现有空素材处理逻辑（跳过本轮 / 告警）
- 任务重置或修改筛选条件时，清空 `delivered_materials`
