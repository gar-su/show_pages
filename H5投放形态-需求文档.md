# H5 投放形态 — 需求文档
## 1. 概述
Meta 自动化任务账户包投放策略由「直投 / W2A」扩展为「直投 / W2A / H5」。H5 形态与 W2A 共用字段集（行动号召 + 像素ID），渠道号移至投放策略下方。
## 2. 背景
业务需支持 H5 落地页投放。当前账户包投放策略为「直投 / W2A」二选一，需扩展 H5 形态；H5 与 W2A 同为落地页投放，字段集一致，仅策略值不同。
## 3. 变更内容
### 3.1 投放策略
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 新增账户包抽屉，投放策略行（`#apStrategy`） | 不变 |
| 选项 | 直投（默认）/ W2A | 直投（默认）/ W2A / H5 |
| 对应值 | `strategy = "直投"` / `"W2A"` | 追加 `"H5"` |
- 切换投放策略实时联动下方字段显隐
### 3.2 H5 字段
H5 与 W2A 共用字段集，选 H5 时显示：
- 行动号召 `#apCta`（落地页文案，≤26 字）
- 像素ID `#apPixelId`
### 3.3 渠道号位置
渠道号（`#apChannelNo`）从推广渠道下方移至投放策略下方、行动号召之前。
新字段顺序：账号包名称、业务类型、推广渠道、投放策略、渠道号、行动号召、像素ID、账户范围。
### 3.4 账户包数据
- `mockAccountPackages` 的 `strategy` 枚举追加 `H5`，部分账户包 mock 改为 H5
- 账户包列表「投放策略」列自然支持 H5 值
### 3.5 提交
账户包创建/提交携带 `strategy`，取值 `直投` / `W2A` / `H5`。
## 4. 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 投放策略加 H5、字段联动、渠道号移位、mock 数据 |
