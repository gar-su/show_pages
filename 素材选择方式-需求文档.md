# PRD-自动化素材配置-新增制定素材模式
## 1. 概述
在 Meta 投放（auto_put）与 TikTok 投放（auto_put_tiktok）两个项目的任务表单中，素材配置新增「素材选择方式」顶层开关，支持筛选模式（原有逻辑）与指定素材（新增逻辑）两种互斥方式。
## 2. 背景
当前素材配置仅支持通过指标条件表达式圈定素材池（筛选逻辑）。部分使用方需要在创建任务时直接指定具体素材，而非依赖指标筛选。两种使用方式互斥，同一任务只能选其一。
## 3. 素材选择方式
### 3.1 模式切换
素材配置标题下方新增按钮组，二选一：
| 按钮文案 | data-mode | 默认 | 行为 |
|---------|----------|------|------|
| 筛选模式 | filter | ✓ | 显示 3.2 筛选字段，隐藏指定素材字段 |
| 指定素材 | specify | | 隐藏 3.2 筛选字段，显示 3.3 指定素材字段 |
### 3.2 筛选模式字段
完全保留现有筛选逻辑，字段不变：
- 素材创建时间（materialTimeItem）
- 素材数据筛选（materialFilterType）→ 自定义时展开：业务类型、筛选媒体、素材投放时间、筛选条件、按指标排序
- 素材数量（materialCountItem）
- 多轮执行不投重复素材（dedupItem）
### 3.3 指定素材字段
筛选字段全部隐藏，替换为：
- **已选素材** — 标签区显示已确认的素材名称，每个标签可移除（×）。初始态显示灰色提示「暂未选择素材，请点击下方按钮添加」
- **「+ 选择素材」按钮** — 打开素材搜索侧边栏
### 3.4 共享字段
以下字段在两种模式下均可见：
- 素材重复次数（repeatCountType）
- 多轮执行不投重复素材（dedupItem）
- 广告数（adCount）
### 3.5 模式切换行为
- 筛选 → 指定：直接切换
- 指定 → 筛选：若已选素材非空，弹出确认「切换至筛选模式将清空已选素材，确定切换吗？」，确认后清空并切换；取消则保持指定模式
- 切换时自动关闭素材预览
## 4. 素材搜索侧边栏
使用当前批量素材库的素材弹窗，保持各项目间一致
## 5. 素材预览
### 5.1 预览按钮
「预览素材」按钮当前隐藏（display:none），相关 JS 逻辑保留但不展示。后续可视需要恢复。
## 6. 提交数据格式
### 6.1 筛选模式
```json
"material": {
  "mode": "filter",
  "createTime": "unlimit",
  "filterType": "custom",
  "businessType": "免费",
  "media": "TikTok",
  "statRange": "7",
  "sortMetric": "compliance",
  "materialCount": "unlimit",
  "conditions": [{"metric": "cost", "operator": ">=", "value": "100"}],
  "repeatCount": "unlimit",
  "dedupAcrossRounds": true,
  "adCount": 30
}
```
### 6.2 指定素材模式
```json
"material": {
  "mode": "specify",
  "items": ["mat_001", "mat_003"],
  "repeatCount": "unlimit",
  "dedupAcrossRounds": true,
  "adCount": 30
}
```
- `items` 为已选素材 ID 数组
- `mode` 字段用于后端区分处理逻辑
## 7. 广告计划创建与轮询机制
### 7.1 任务基础配置（两种模式共用）
每个自动化任务按以下参数周期性执行：
- **执行频率**（executeFrequency）：分钟级定时触发
- **短剧选择**（dramas）：已选短剧列表，每个短剧绑定一个或多个定向包
- **投放账户**（account.package）：账户包，展开为具体账户列表
- **单轮单短剧计划创建上限**（campaignLimit）：每轮每个短剧最多创建 N 个广告系列
- **结束运行条件**（endConditions）：累计投放短剧数 / 账户数达到阈值时停止
- **广告数**（adCount）：每个广告系列下的广告数量，不填默认 30
### 7.2 筛选模式 — 每轮素材获取
每轮执行时：
1. 以当前时间为基准，按素材创建时间窗口筛选素材
2. 若素材数据筛选 = 自定义，进一步按业务类型/媒体/投放时间窗口/条件表达式过滤
3. 按排序指标对结果排序
4. 素材数量 = 不限时取全部，自定义时取前 N 条
5. 若开启多轮去重，排除前序轮次已使用的素材
6. 得到本轮素材池 `P`

### 7.3 指定素材模式 — 每轮素材获取
每轮执行时：
- 不查询素材库，直接使用任务创建时已确认的素材列表 `items[]` 作为候选池
- 若开启多轮去重（dedupAcrossRounds），排除前序轮次已使用过的素材，得到本轮素材池 `P`
- 素材重复次数控制池内素材的复用：
  - **不限**：每个素材每轮可用 1 次
  - **自定义 N**：每个素材每轮最多可用 N 次，`P` 池按重复次数展开为 `P_expanded`
- 池内素材随轮次推进逐步消耗，耗尽后该任务不再创建新广告

### 7.4 广告计划生成（两种模式统一）
素材池 `P`（或展开后的 `P_expanded`）确定后，后续流程一致：

```
for each 短剧 in dramas:
    for each 定向包 in 短剧.定向包列表:
        for each 账户 in 定向包对应的账户列表:
            for each 素材 in P (按池内顺序):
                创建广告系列（含广告组 + 广告）
                累计计数
                if 当前短剧本轮计划数 >= campaignLimit: break
```

- 素材按池内顺序分配，先到先得
- campaignLimit 逐短剧独立计数
- 广告数决定每个广告系列下的广告条数

### 7.5 两种模式对比
| 维度 | 筛选模式 | 指定素材模式 |
|------|---------|------------|
| 素材来源 | 每轮动态查询 | 任务创建时固定 |
| 素材变化 | 随素材库新增/数据波动变化 | 始终不变 |
| 跨轮去重 | 支持（dedupAcrossRounds） | 支持（dedupAcrossRounds），逐步消耗固定池 |
| 素材重复次数 | 控制每轮的最大使用次数 | 同上 |
| 适用场景 | 自动化跑量，依赖指标择优 | 人工精选，定向投放指定素材 |
## 8. 表单重置
关闭任务弹窗或新建任务时：
- 若当前为指定模式，自动切回筛选模式
- 清空 `confirmedMaterials`、`drawerSelectedIds`
- 恢复素材选择方式按钮组默认选中状态
## 9. 影响范围
| 项目 | 文件 | 变更 |
|------|------|------|
| auto_put_tiktok | index.html | 新增模式切换、指定素材字段、素材搜索侧边栏；隐藏预览按钮；mock数据增加 id 字段 |
| auto_put | index.html | 同上，媒体下拉使用 Meta 替代 Facebook |
## 10. 实现说明
- 素材搜索侧边栏复用现有 Drawer 样式体系，新增 `material-drawer-*` 前缀样式
- 素材标签复用 `.material-tag` 类，与定向包标签 `.drama-targeting-tag` 视觉一致
- 抽屉内选中状态通过 `drawerSelectedIds` (Set) 维护，跨页不丢
- 主表单 `confirmedMaterials` 为确认后的素材对象数组，提交时映射为 ID 数组
