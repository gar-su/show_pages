# Meta自动化任务 PRD

## 2. 选择短剧模块

### 2.1 页面结构

选择短剧区域包含：
- 搜索模式切换 Tab（按短剧搜索 / 按剧本搜索）
- 搜索输入框与筛选条件
- 搜索结果列表
- 已选短剧展示区

### 2.2 搜索模式切换

| 模式 | 筛选条件 | 输入方式 | 搜索结果 |
|------|----------|----------|----------|
| 按短剧搜索 | 短剧名称 | 多行输入，每行一个短剧名，支持同时搜索多个短剧 | 返回匹配的短剧列表 |
| 按剧本搜索 | 剧本ID 或剧本名 + 语言（可选） | 多行输入，每行一个剧本ID或剧本名，支持同时搜索多个剧本 | 返回匹配的短剧列表 |

说明：
- 语言在按剧本搜索时为可选筛选条件
- 不选择语言时，返回该剧本下所有语言的短剧
- 两个模式的输入框均支持多行文本输入，换行分隔，空行自动忽略

### 2.3 搜索结果字段

| 字段 | 说明 |
|------|------|
| 剧本名称 | 剧本的中文名称 |
| 剧本ID | 剧本唯一标识 |
| 短剧名称 | 短剧的本地化名称（随语言变化） |
| 语言 | 短剧对应的语言 |

语言枚举值：

| 代码 | 语言 |
|------|------|
| en_US | 英文 |
| zh_CN | 中文 |
| ja_JP | 日文 |
| zh_TW | 繁体中文 |
| ko_KR | 韩文 |
| es_ES | 西班牙文 |
| th_TH | 泰文 |
| id_ID | 印尼文 |
| pt_PT | 葡萄牙文 |
| fr_FR | 法文 |
| de_DE | 德文 |
| it_IT | 意大利文 |
| ar_AE | 阿拉伯文 |
| ms_MY | 马来文 |
| vi_VN | 越南文 |
| tr_TR | 土耳其文 |
| hi_IN | 印地文 |

### 2.4 已选短剧展示

展示格式：`剧本ID-剧本名称-短剧名称-语言`

示例：
- L0001-繁花-Blooming-英文
- L0001-繁花-Mekar-马来文
- L0001-繁花-บุษบา-泰文

操作：
- 单个移除：点击 x 按钮
- 清空全部：点击"清空全部"按钮

---

## 3. 字段说明

### 3.1 数据模型关系

```
剧本 
├── 剧本ID：L0001
├── 剧本名称：繁花
└── 短剧列表 
    ├── 语言：中文 → 短剧名称：繁花
    ├── 语言：英文 → 短剧名称：Blooming
    ├── 语言：泰文 → 短剧名称：บุษบา
    └── 语言：马来文 → 短剧名称：Mekar
```

---

## 4. 交互状态说明

### 4.1 搜索按钮状态

| 条件 | 状态 |
|------|------|
| 必填项为空 | 按钮禁用 / 提示用户输入 |
| 搜索中 | 显示加载中 |
| 搜索完成 | 显示结果列表 |

### 4.2 添加按钮状态

| 状态 | 样式 |
|------|------|
| 未添加 | 蓝色边框 + 蓝色文字 |
| 已添加 | 灰色 + "已添加"文字，按钮禁用 |

### 4.3 空状态

- 已选短剧为空时：显示"请添加短剧"提示
- 搜索结果为空时：显示"未找到匹配的短剧"

---

## 5. 验收标准

1. 支持按短剧名称搜索短剧（多行输入，支持同时搜索多个）
2. 支持按剧本ID或剧本名搜索（多行输入，支持同时搜索多个）
3. 按剧本搜索时，语言为可选筛选条件
4. 不选语言时，搜索结果返回多条短剧
5. 搜索结果展示剧本名称、剧本ID、短剧名称、语言
6. 已选短剧展示格式：剧本ID-剧本名称-短剧名称-语言
7. 支持单个移除和清空全部
8. 同一短剧不可重复添加
