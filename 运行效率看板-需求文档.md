# 运行效率看板-需求文档
## 1. 概述
在 Meta 自动化任务（auto_put）与 TikTok 自动化任务（auto_put_tiktok）两个项目中，为任务列表页新增任务运行效率概览卡，让运营与主管直接看到自己及下属任务建出来的广告实体有多少成功上传，不必逐个任务翻日志。
同时对齐两项目的任务列表筛选项现状：补齐创建人下拉数据、新增创建时间与更新时间日期区间筛选、概览卡跟随筛选联动。
两个项目的概览卡一致，均为四张：已启用任务、广告计划（TikTok 侧为广告系列）上传成功率、广告组上传成功率、广告上传成功率。不新增页面、不做趋势图、不做下钻。
## 2. 背景
两个项目的任务列表当前都只有配置态信息（任务名、状态、竞价策略、日预算、执行间隔、创建人、上次完成时间等），没有任何运行结果态。任务建出来的广告计划、广告组、广告有没有成功传上去，页面上完全看不出来，运营只能逐个任务翻日志。
广告实体的上传状态是逐层继承的：广告计划、广告组、广告都可能独立失败，任何一层失败都意味着这条链路最终没投出去。因此需要分三层分别观察成功率，才能定位失败发生在哪一层，而不是只看一个笼统的整体数字。
筛选侧两个项目都存在同类问题：创建人下拉为空壳不可用，缺少按时间维度的筛选能力，任务列表页的筛选控件与列表数据未建立联动。
## 3. 通用功能（两项目一致）
### 3.1 改动范围
| 项 | 内容 |
|----|------|
| 位置 | 自动化任务 › 任务 列表页 |
| 新增 | 筛选工具栏下方一排 4 张效率概览卡 |
| 调整 | 创建人下拉补齐数据；新增创建时间、更新时间日期区间筛选；概览卡与列表联动 |
| 不变 | 任务列表表格与分页均不变 |
不新增导航项、不新增独立页面。
### 3.2 布局顺序
页签下方自上而下依次为：筛选工具栏、效率概览卡、任务列表表格、分页。
### 3.3 概览卡清单
| 卡片 | 回答的问题 | 指标口径 | 数据来源 |
|------|-----------|---------|---------|
| 已启用任务 | 我名下有多少任务在跑 | 状态为「启用」的任务数 | 任务 |
| 广告计划上传成功率 | 建出来的计划传上去了吗 | 上传成功的广告计划数 ÷（成功 + 失败）计划数 | 广告计划 |
| 广告组上传成功率 | 建出来的广告组传上去了吗 | 上传成功的广告组数 ÷（成功 + 失败）广告组数 | 广告组 |
| 广告上传成功率 | 建出来的广告传上去了吗 | 上传成功的广告数 ÷（成功 + 失败）广告数 | 广告 |
### 3.4 展示形式
- 四张卡横向等宽平铺，单行排列
- 每张卡由标签、数值、辅助说明三部分组成
- 已启用任务卡显示任务数（整数）；其余三张卡显示百分比，保留一位小数
- 辅助说明显示该指标的构成明细，用于判断数值来源
### 3.5 上传成功率口径
**统计范围**——先按筛选项确定任务范围，再取这些任务名下的广告计划 / 广告组 / 广告实体进行统计。切换筛选项时三张卡同步收敛。三层使用同一套上下级归属关系：广告计划归属于任务，广告组归属于广告计划，广告归属于广告组。
**上传状态的取值与分母**——上传状态取值为「待上传 / 上传成功 / 上传失败」。
- 分子：上传状态为「上传成功」的条数
- 分母：上传状态为「上传成功」与「上传失败」的条数之和
- 「待上传」不计入分母（语义上它还没有结果），但需在辅助说明中单独展示待上传条数，避免全部条目停在待上传时比率显示为无数据而看不出异常
- 某层不存在待上传状态时，辅助说明中不展示该项
**统计时点**——三张卡按各广告实体当前的上传状态做快照统计，不限定时间范围。上传状态是持续状态（失败的计划不会因为时间过去而变成成功），套用时间窗口会把较早的失败排除在统计之外，掩盖问题。
### 3.6 交互规则
- 仅统计当前登录用户本人及其下属创建的任务，复用后台已有权限控制，不额外配置
- 概览卡随筛选项联动：任一筛选项生效后，卡片统计范围同步收敛为筛选结果，三层上传成功率随之按筛选出的任务重新汇总
- 点击「查询」应用全部筛选项，概览卡与列表同步刷新
- 点击「重置」清空全部筛选项，概览卡与列表恢复全量口径
- 概览卡为只读展示，不可点击，不触发筛选或跳转
- 卡片不参与列表分页，不受当前页码影响，始终统计全部筛选结果
### 3.7 边界与异常
| 场景 | 表现 |
|------|------|
| 某一层不存在上传成功与失败的记录（成功 + 失败为 0） | 该卡显示 -- |
| 某一层全部条目停在待上传 | 该卡显示 --，辅助说明展示待上传条数 |
| 筛选结果为空 | 已启用任务显示 0，其余三张卡显示 --，列表展示空 |
| 某任务名下没有广告计划 / 广告组 / 广告 | 该层按 0 条计入，不影响其他层 |
| 比率类指标分母为 0 | 一律显示 --，不得显示 0% 或 NaN |
| 日期区间只填一端 | 按单端条件过滤，不报错 |
| 日期区间开始晚于结束 | 结果为空，不报错 |
## 4. 层级命名差异（两项目不同）
两个项目的后台层级命名不同，需求文档与界面文案须各自使用本项目的术语，不得混用：
| 层级 | Meta 项目（auto_put） | TikTok 项目（auto_put_tiktok） |
|------|---------------------|------------------------------|
| Campaign | 广告计划 | 广告系列 |
| Ad Set | 广告组 | 广告组 |
| Ad | 广告 | 广告 |
因此概览卡第二张在两个项目中的名称不同：Meta 侧为「广告计划上传成功率」，TikTok 侧为「广告系列上传成功率」。其余三张卡名称一致。
## 5. Meta 项目（auto_put）
### 5.1 筛选项清单
| 筛选项 | 控件 | 说明 |
|--------|------|------|
| 任务ID | 文本输入 | 模糊匹配 |
| 任务名称 | 文本输入 | 模糊匹配 |
| 任务状态 | 下拉 | 启用 / 停用 |
| 竞价策略 | 下拉 | 见 5.2 |
| 创建人 | 下拉 | 选项为当前用户可见范围内的成员 |
| 创建时间 | 日期区间 | 开始日期至结束日期 |
| 更新时间 | 日期区间 | 开始日期至结束日期 |
筛选项以实际支持范围为准，不再展示无效筛选项。
### 5.2 竞价策略与参数输入
竞价策略下拉的选项为以下四项：最高数量、单次成效费用目标、竞价上限、广告花费回报目标。
选中某一策略后，仅显示该策略对应的参数输入框：
| 竞价策略 | 对应参数输入框 |
|----------|---------------|
| 最高数量 | 无 |
| 单次成效费用目标 | 单次成效费用目标 |
| 竞价上限 | 竞价上限 |
| 广告花费回报目标 | 广告花费回报目标 |
未选择策略、或选择「最高数量」时，三个参数输入框均不显示。切换策略时同步切换对应输入框，已填写的其他参数不保留。
## 6. TikTok 项目（auto_put_tiktok）
### 6.1 筛选项清单
| 筛选项 | 控件 | 说明 |
|--------|------|------|
| 任务ID | 文本输入 | 模糊匹配 |
| 任务名称 | 文本输入 | 模糊匹配 |
| 任务状态 | 下拉 | 启用 / 停用 |
| 创建人 | 下拉 | 选项为当前用户可见范围内的成员 |
| 创建时间 | 日期区间 | 开始日期至结束日期 |
| 更新时间 | 日期区间 | 开始日期至结束日期 |
TikTok 任务列表页当前没有竞价策略筛选项，本期不新增。筛选项以实际支持范围为准。
### 6.2 概览卡文案差异
概览卡第二张在 TikTok 侧命名为「广告系列上传成功率」（见第 4 节），其余三张与通用定义一致。
## 7. 验收标准
### 7.1 两项目通用
- 页面自上而下为筛选工具栏、概览卡、列表表格、分页，顺序正确
- 概览卡共 4 张，分别为已启用任务、广告计划（TikTok 侧为广告系列）上传成功率、广告组上传成功率、广告上传成功率
- 三张上传成功率的数值与按当前筛选结果手算的结果一致，且与「广告系列 / 广告组 / 广告」三个下钻页中对应实体的上传状态分布一致
- 三张卡的分母均排除「待上传」条目；存在待上传条目时，辅助说明中可见其条数
- 某一层无待上传状态时，该卡辅助说明不出现待上传项
- 创建人下拉已填充可选项，选择任一成员后列表与卡片同步收敛
- 创建时间、更新时间可分别按日期区间过滤，卡片与列表同步变化
- 按任务ID筛选到单个任务后，三张上传成功率卡片显示为该任务名下三层实体的上传口径
- 点击「重置」后全部筛选项清空，卡片恢复全量口径
- 筛选出空结果时，比率卡显示 -- 而非 0% 或 NaN
- 切换到其他页面再切回，概览卡仍正常展示
### 7.2 Meta 项目
- 筛选项共 7 个：任务ID、任务名称、任务状态、竞价策略、创建人、创建时间、更新时间
- 竞价策略下拉为四个子选项；选择「竞价上限」时仅显示竞价上限输入框，「最高数量」时三个参数框均不显示
- 将任务状态筛选为「停用」并查询后，卡片数值与列表条数同步变化
### 7.3 TikTok 项目
- 筛选项共 6 个：任务ID、任务名称、任务状态、创建人、创建时间、更新时间
- 任务列表页不出现竞价策略筛选项
- 概览卡第二张文案为「广告系列上传成功率」，不得出现「广告计划」
## 8. 不在本期范围
- 投放效果指标（消耗、ROI、达标率等）
- 执行到达率及「跑得顺不顺」维度。该指标需要按任务执行时段与执行间隔推算应执行轮次，依赖于任务时段配置的准确性；本期不做
- 计划达成率及「跑得满不满」维度。原设计的计划达成率以「按素材可用情况推算的可创建系列数」为分母，存在口径陷阱：素材池越少、分母越低，素材枯竭的任务反而显示高达成率，与暴露空转的目的相反。本期不补
- 素材耗尽、账户满员、执行异常等空转原因的归因展示
- 上传失败的失败原因汇总与批量重试入口
- 独立看板页面与导航项
- 按天趋势图、时间范围筛选控件
- 卡片点击下钻、跳转任务列表筛选
- TikTok 侧竞价策略筛选项（该页当前没有，本期不引入）
## 9. 影响文件
| 项目 | 仓库 | 文件 | 说明 |
|------|------|------|------|
| Meta 投放 | auto_put | index.html | 任务列表页新增四张概览卡、筛选项收敛与补齐、竞价策略参数条件显隐；广告计划 / 广告组 / 广告三张列表数据与任务建立归属关系 |
| TikTok 投放 | auto_put_tiktok | index.html | 任务列表页新增四张概览卡、筛选项补齐（创建人与日期区间）；广告系列 / 广告组 / 广告三张列表数据与任务建立归属关系 |
