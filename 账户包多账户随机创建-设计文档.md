# 素材分发与生命周期 — 需求文档
## 1. 背景与目标
现有自动化任务存在两个能力缺口：素材「重复次数」配置从未真实生效（仅参与缓存 key，不影响投放分配）；同一条素材无法在一个任务里铺到多个账户。TikTok 任务当前仅单账户创建。本次将投放模型重构为**素材驱动**：筛一份素材池，池内素材按「单轮投放次数 × 累计投放次数」分发到账户，优先分散投放、避免同一素材集中堆叠在单一账户。
目标：
- 让「素材可投几次」成为真实生效的投放控制。
- 一条素材可在生命周期内铺到多个账户（跨账户铺量）。
- 用「累计投放次数」控制素材生命周期，投满即该素材退出投放。
- 每轮可限定参与账户数量，账户按包内顺序、跳过已满员账户，替换原「随机抽取」设计，保证结果可预期、可解释。
## 2. 核心模型：一份池、素材主导
任务 → 短剧 → 素材池 → 账户。素材是投放主体，账户是承接对象。
- **一份素材池**：每个短剧按配置（规则/指定素材）筛出一份素材池，全部账户共用，不存在「每账户各自筛一套」。
- **素材主导**：池内每条素材按自己的投放预算（单轮投放次数、累计投放次数）被分发到账户；素材进入系列即产生一条广告。
- **分散优先**：同一素材的多次投放尽可能落到不同账户，避免同一账户内出现该素材的重复系列；账户数不足时才回填已用账户。
- **账户侧广度**：「每轮账户数」独立于素材预算，限制本轮参与建系列的账户数量。
## 3. 字段与语义
| 字段 | 类型 | 必填 | 默认 | 语义 |
|------|------|------|------|------|
| 账户包 | 下拉 | 是 | — | 提供候选账户 |
| 每轮账户数 | 数字输入 | 否 | 5 | 本轮参与建系列的账户上限，范围 1~包内账户总数；清空=全部账户 |
| 单轮单短剧计划创建上限 | 数字输入 | 否 | 不填=不限 | 兜底上限：本轮单个短剧最多建几个系列。不填=系列数仅由素材预算与素材池决定，不额外封顶；填 N=本轮该短剧最多建 N 个系列 |
| 素材单轮投放次数 | 选择 | 否 | 自定义=1 | 一条素材单轮（跨账户合计）最多进入几个系列；自定义=1 时每轮只进 1 个系列；不限=无上限 |
| 素材累计投放次数 | 选择 | 否 | 自定义=1 | 一条素材全生命周期（跨轮、跨账户合计）最多进入几个系列；达到后该素材退出投放 |
删除旧「素材重复次数」「多轮投不重复素材」两控件；素材与账户的已投记账均以「素材 × 账户」为粒度，记录某条素材在某账户已进过几个系列。
## 4. 投放与分散策略
### 4.1 分发规则
- 素材按「单轮投放次数」决定本轮可进系列数，按「累计投放次数」决定剩余生命周期（剩余 = 累计次数 − 已进系列总数）。
- 本轮实际可进系列数 = min(单轮次数, 累计剩余, 池内可用性)。
- 每条素材进入不同系列的投放中，**优先选择该素材尚未占用过的账户**；若「每轮账户数」或账户包规模不足以容纳，才让同一素材进入同一账户的不同系列。
- 分散是均衡约束：同一素材在多个账户间的占用数尽量平均，避免个别账户承接过多该素材。
### 4.2 示例（单轮=1，累计=3，账户包甲/乙/丙，每系列 1 条素材）
| 轮次 | 素材去向 | 累计占用 |
|------|------|------|
| 第 1 轮 | 进 1 个系列 → 账户甲 | 甲1 |
| 第 2 轮 | 累计 1<3，进 1 个系列 → 优先未占用账户 → 账户乙 | 甲1 乙1 |
| 第 3 轮 | 累计 2<3 → 账户丙 | 甲1 乙1 丙1 |
| 第 4 轮 | 累计 3=累计次数 → 退出投放 | — |
覆盖 3 个账户各 1 次，任一账户未出现重复系列。
### 4.3 示例（单轮=2，累计=6，账户甲/乙/丙）
第 1 轮素材进 2 个系列 → 甲、乙各 1。第 2 轮仍进 2 个系列 → 丙已占用 0（最少）取 1，剩余 1 次回填占用最少的甲或乙 → 总体各账户占用趋于均衡：最终甲/乙/丙各 2。
## 5. 覆盖关系（相对旧配置）
| 新配置（单轮, 累计） | 行为 |
|------|------|
| 单轮 1, 累计 1 | 每素材只进 1 个系列后退出（最保守，不铺量） |
| 单轮 1, 累计不限 | 每素材每轮进 1 个系列，跨轮可换账户铺开，永不重复同账户同轮 |
| 单轮多次, 累计不限 | 每素材单轮最多进多系列（可分散到多个账户），跨轮无限复用 |
| 单轮多次, 累计 N | 每素材生命周期内最多进 N 个系列，单轮最多几个，投满退出 |
| 单轮 1, 累计 N | 每素材每轮 1 个系列、累计最多 N 个，分散至多 N 个账户 |
说明：旧「素材重复次数」从未在投放分配中生效，本方案首次使其成为真实投放控制，并非等价迁移。
## 6. 终止与边界
- 一轮停止条件：素材池发完、本轮账户全部满员、素材预算（单轮或累计次数）用尽、每轮账户数/计划创建上限触达。
- 「不要集中堆叠」不是硬拒绝：当账户数 < 素材需投放次数时，允许素材回填已用账户，以占用均衡为约束。
- 素材退出（达到累计次数）仅影响该素材，不影响其他素材与其他账户。
## 7. 现状基线（两平台不同，需各自对齐）
| 平台 | 现状 |
|------|------|
| auto_put_tiktok（本文档） | 任务当前仅单账户创建，需扩展为多账户参与 |
| auto_put (Meta) | 现状为账户驱动双层循环：外层系列数、内层账户遍历，素材按「任务+短剧」去重；需调整为素材预算模型并引入账户维度 |
本需求核心语义（一份池、素材主导、分散优先、素材生命周期）两平台通用，各平台按各自基线实施。
## 8. 校验与联动
| 联动项 | 语义 |
|--------|------|
| 单轮单短剧计划创建上限 | 本轮该短剧总系列数兜底上限，与素材预算/素材数量取小 |
| 每系列素材数 | 一个系列取多少条素材，决定一条素材能否在相邻系列复用 |
| 素材数量自定义 | 限制池大小；规则查询缓存共享，命中缓存仍 shuffle |
| 结束条件（单账户在投系列数/账户数） | 保留现有账户维度停止条件，与素材预算正交 |
| 每轮账户数 | 限本轮参与账户广度；默认 5，清空=全部账户参与 |
校验：每轮账户数范围 1~包内账户总数，越界报错；账户包无账户报错；计划创建上限填了须 ≥1；商品库/身份匹配校验「本轮参与账户 × 短剧」，缺失组合截断该账户并告警，不阻断其他账户。
## 9. 待确认事项
1. 存量已投素材（无账户维度）如何归属，建议视为该素材已投原任务全局一次，避免重复铺老素材，需产品拍板。
2. 分散均衡的实现粒度：优先「素材占用账户数最少」，还是「账户占用素材总数最少」两种口径可由产品选定，默认取后者更贴合「不搭」诉求。
## 10. 验收标准
1. 单轮投放次数=1 时，同一素材同一轮只进 1 个系列；不同账户各承接一次（多轮铺开）。
2. 单轮投放次数=2 时，同一素材一轮进 2 个系列并优先落到不同账户。
3. 累计投放次数=3：素材覆盖 3 个账户各 1 次后退出，后续轮次不再出现。
4. 同一素材在同一账户不出现重复系列（账户足够时）。
5. 每轮账户数默认 5、包内 10 账户：本轮最多 5 个账户参与建系列，清空则全部参与。
6. 账户包无账户、每轮账户数越界时阻止提交并提示；计划创建上限不填可正常提交。
7. 身份匹配按本轮参与账户校验，缺失组合截断该账户并告警，不阻断其他账户。