# 排期参数做到短剧上-需求文档
## 1. 概述
将媒体侧排期参数由任务级下沉到**短剧级**（每个短剧独立排期），Meta 自动化任务（auto_put）与 TikTok 自动化任务（auto_put_tiktok）均调整为短剧级排期。本需求独立于「预上线短剧与媒体排期-需求文档.md」（该需求保留任务级单字段版本，不受影响）：
| 项目 | 原排期 | 新排期 | 提交扩展 |
|------|--------|--------|---------|
| Meta（auto_put） | 无排期功能 | 短剧级，每个短剧行独立 | dramas[] 追加 status 与 scheduleTime |
| TikTok（auto_put_tiktok） | 任务级「排期（东八区）」单字段 | 短剧级，每个短剧行独立 | 移除任务级 scheduleTime，dramas[] 追加 status 与 scheduleTime |
- 前置依赖：「预上线短剧支持」（短剧搜索覆盖已上线+预上线、排除待上线）——TikTok 已具备，Meta 侧需随本需求一并补齐（见 5.1）
## 2. 背景
业务按短剧维度差异化编排排期：预上线短剧需提前建单、到点后立即起量；已上线短剧可单独插入延后投放。任务级单字段无法表达「同一任务内各短剧排期不同」，故将排期下沉到短剧级，各短剧独立设置。
## 3. 通用功能（两项目一致）
### 3.1 排期参数（短剧级）
已选短剧区每个短剧行内均提供「排期（东八区）」输入（各短剧独立），输入形式为日期 + 具体时间，**按东八区填写**，位于该短剧定向包下方。
- 行内提示「留空=立即投放」
- 已选短剧区顶部统一说明：「已选预上线短剧时，该短剧排期必填；已上线短剧留空=立即投放；时间按东八区填写」
### 3.2 必填规则
| 短剧状态 | 该短剧排期（东八区） |
|---------|-----------|
| 预上线 | 必填 |
| 已上线 | 选填，留空 = 立即投放 |
### 3.3 投放机制
- 各短剧排期时间确定后，任务创建时即生成该短剧的监测链接与广告计划
- 到期时间点之前广告计划处于「待投放」状态，不消耗预算
- 到达排期时间点后，媒体侧自动开启投放
### 3.4 时区转换
- 各短剧排期时间以东八区（UTC+8）为基准填写，即系统统一时区
- **账户级时区**：不同投放账户可能处于不同时区，创建广告计划时，需将该短剧排期时间从东八区**转换为该投放账户所在时区**，转换后的时间作为该账户广告计划的媒体排期参数
- 示例：某短剧排期 2026-08-01 10:00（东八区），某账户时区为东五区，则该账户下该短剧广告计划排期为 2026-08-01 07:00（账户时区）
## 4. 提交数据与校验（两项目一致）
### 4.1 提交数据
提交时 `dramas[]` 数组每项扩展：
```json
{
  "dramas": [
    {"key": "L0001-繁花-Mekar-马来文", "status": "预上线", "targetingPackages": [], "scheduleTime": "2026-08-01T10:00+08:00"}
  ]
}
```
- `dramas[].status`：所选短剧的上线状态（已上线 / 预上线），用于后端区分是否触发排期逻辑
- `dramas[].scheduleTime`：该短剧排期时间，ISO 格式并**携带东八区时区偏移（+08:00）**；已上线短剧可缺省（null 表示立即投放）
- 后端创建广告计划时，将该短剧 `scheduleTime` 按各投放账户所在时区转换后使用（见 3.4）
### 4.2 提交校验
- 选择含预上线短剧且该短剧排期为空 → 拦截并提示填写该短剧排期时间
## 5. Meta 项目（auto_put）
### 5.1 前置：预上线短剧支持
- 短剧库数据含「上线状态」字段，取值：已上线 / 预上线 / 待上线
- 短剧搜索仅返回**已上线 + 预上线**，排除待上线；不做状态筛选控件；搜索卡片不展示上线状态与预计上线时间
| 模式 | 匹配字段 | 状态范围 |
|------|---------|---------|
| 按短剧搜索 | 短剧名称 | 已上线 + 预上线（排除待上线） |
| 按剧本搜索 | 剧本ID / 剧本名（可选语言） | 已上线 + 预上线（排除待上线） |
- 无命中时展示占位提示
### 5.2 排期改造
- 每个短剧行内新增「排期（东八区）」输入；原无任务级排期字段，无需移除
### 5.3 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 新增短剧库（含上线状态）、搜索改为从库匹配并排除待上线、短剧行内排期输入、校验与提交扩展 |
## 6. TikTok 项目（auto_put_tiktok）
### 6.1 变更内容
| 项 | 原 | 新 |
|----|-----|-----|
| 排期维度 | 任务级「排期（东八区）」单字段 | 短剧级，移至每个短剧行内 |
| 短剧行 | 短剧名 + 定向包 | 追加排期（东八区）输入（短剧级） |
| 提交 | 任务级 scheduleTime 字段 | 移除任务级字段，改为 dramas[] 内 status 与 scheduleTime |
### 6.2 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 移除任务级排期字段，新增短剧行内排期输入、校验与提交扩展 |