# 选择H5 — 需求文档
## 1. 概述
投放策略选 H5 后，需指定实际投放的具体 H5（落地页），新增「选择H5」必填下拉。待选 H5 名单由监测链接接口按 投放策略 × 业务类型 维度返回，仅 H5 形态下展示。Meta 自动化任务（auto_put）与 TikTok 自动化任务（auto_put_tiktok）均需补充，两项目分别调整：
| 项目 | 表单位置 | 字段位置 | 显示条件 |
|------|---------|---------|---------|
| Meta | 「新增账户包」侧边栏 | 「投放策略」之后、「回传模板」之前 | 投放策略 = H5 |
| TikTok | 任务表单投放配置区 | 「投放策略」之后、「渠道号」之前 | 推广目标 = 销量 且 投放策略 = H5 |
## 2. 背景
H5 投放形态下广告点击进入 H5 落地页，不同 H5 对应不同落地页与转化配置，投放前须指定投放哪个 H5。当前 Meta「新增账户包」与 TikTok 任务表单选 H5 后均无 H5 选择录入，需补充。可投放的 H5 名单由监测链接接口提供，字段侧仅需携带接口返回的选项，拆分字段口径不在本次需求内定义。
## 3. Meta 项目（auto_put）
### 3.1 变更内容 — 新增账户包侧边栏
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 新增账户包侧边栏（`#accountPackageDrawer`） | 不变 |
| 字段 | 选 H5 后无 H5 选择录入 | 「投放策略」之后新增「选择H5」下拉（必填，无默认） |
- 可见条件：投放策略 = H5 时显示；直投 / W2A 时隐藏并清空已选项
### 3.2 下拉数据来源
待选名单由监测链接接口按以下参数筛选：
| 参数 | 取值 | 说明 |
|------|------|------|
| `strategy` | `4` | 仅 H5（promotionType=4），与「投放策略」按钮组联动 |
| `businessType` | `1`=免费 / `2`=付费 | 与账户包「业务类型」一致 |
- 投放策略 = H5 且 业务类型无匹配 H5 时，下拉仅剩占位项
- 选项展示 H5 名称，占位文案「请选择H5」
### 3.3 表单联动
- 切换投放策略 / 业务类型时重新请求名单并清空已选项
- 打开「新增账户包」抽屉时按当前策略与业务类型初始化名单
### 3.4 提交
账户包创建时携带所选 H5（随账户包数据提交，拆分字段口径以监测链接接口为准）。
### 3.5 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| Meta 投放 | index.html | 账户包侧边栏新增「选择H5」下拉、联动逻辑、提交校验、mock 名单 |
## 4. TikTok 项目（auto_put_tiktok）
### 4.1 变更内容 — 投放配置区
| 项目 | 原 | 新 |
|------|-----|-----|
| 位置 | 「投放策略」之后、「渠道号」之前 | 不变 |
| 字段 | 销量选 H5 后提交自动拼装 monitorLink，无 H5 选择录入 | 「投放策略」之后新增「选择H5」下拉（必填，无默认） |
- 可见条件：推广目标 = 销量 且 投放策略 = H5 时显示；其余隐藏并清空已选项
### 4.2 下拉数据来源
待选名单由监测链接接口按以下参数筛选：
| 参数 | 取值 | 说明 |
|------|------|------|
| `strategy` | `4` | 仅 H5（promotionType=4），与「投放策略」按钮组联动 |
| `businessType` | `1`=免费 / `2`=付费 | 与任务业务类型一致 |
- `strategy` 不在 H5（W2A / 应用推广直投）时不显示该字段
- 选项展示 H5 名称，占位文案「请选择H5」
### 4.3 表单联动
- 切换推广目标 / 投放策略 / 业务类型时重新请求名单并清空已选项
- 任务表单初始化时不显示该字段
### 4.4 提交
任务提交时 H5 形态必选，未选拦截提醒；所选 H5 随任务提交（拆分字段口径以监测链接接口为准）。
### 4.5 影响文件
| 项目 | 文件 | 说明 |
|------|------|------|
| TikTok 投放 | index.html | 投放配置区新增「选择H5」下拉、联动逻辑、提交校验、mock 名单 |