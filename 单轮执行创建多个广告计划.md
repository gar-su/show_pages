# 单轮执行创建多个广告计划
## 概述
定时任务单轮执行时支持创建多个广告计划，不再限制为 1 个计划。页面新增可选的计划上限配置项。
## 页面改动
### 位置
基本信息区域，结束运行条件下方。
### 字段
| 属性 | 值 |
|------|-----|
| 字段名 | `campaignLimit` |
| 类型 | number / null |
| 组件 | 数字输入框 |
| 最小值 | 1 |
| 最大值 | 30 |
| 默认 | 1 |
| 说明 | 必填字段，限制单轮执行最多创建的广告计划数 |
### 交互
- 用户输入 1-30 之间的整数
- 输入超出范围时浏览器原生校验拦截
- 不填写时提交 `null`，后端不限制计划数量
## 广告结构
### 当前结构（1:1:1）
单轮执行时，每个短剧 → 1 个广告计划 → 1 个广告组 → 1 个广告。
### 目标结构（1:1:n）
单轮执行时，每个短剧 → 1 个广告计划 → 1 个广告组 → n 个广告。n 为素材配置中设置的广告数（`adCount` 字段）。
### 说明
- 广告计划数量由 `campaignLimit` 控制上限
- 每个计划内仍为 1 个广告组、n 个广告
- n 的值取自素材配置模块的「广告数」字段
## 提交数据格式
```json
{
  "campaignLimit": 5
}
```
## 验收标准
1. 结束运行条件下方显示「单轮单短剧计划创建上限」字段
2. 输入框限制 1-30，超范围时浏览器阻止提交
3. 不填写或超范围时浏览器+JS 双重校验阻止提交
4. 单轮执行广告结构为 1:1:n（n 为广告数配置值）
